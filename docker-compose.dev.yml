version: '3'

volumes:
  portainer_data:

services:
  inpe_cdsr_nginx:
    container_name: inpe_cdsr_nginx
    image: nginx:1.17.6
    # restart: always
    volumes:
      - ./volumes/nginx/nginx.dev.template:/etc/nginx/conf.d/mysite.template
      - /data/TIFF:/var/www/html/datastore/TIFF
      # - ../catalog-dash/assets:/var/www/html/assets
    env_file:
      - ./env_files/nginx.env
    command: /bin/bash -c "envsubst '$$NGINX_HOST $$NGINX_PORT' < /etc/nginx/conf.d/mysite.template > /etc/nginx/conf.d/default.conf && nginx -g 'daemon off;'"
    networks:
      - inpe_cdsr
    depends_on:
      - inpe_cdsr_catalog_frontend
      - inpe_cdsr_catalog_backend
      - inpe_cdsr_catalog_dash
      - inpe_cdsr_inpe_stac
      - inpe_cdsr_stac_compose
      - inpe_cdsr_portainer
      - inpe_cdsr_geoserver
    ports:
      - 8089:8089

  inpe_cdsr_catalog_frontend:
    container_name: inpe_cdsr_catalog_frontend
    image: inpe-cdsr-catalog-frontend
    command: npm start
    # restart: always
    volumes:
      - ../catalog-frontend/portal:/app
      - ./volumes/catalog-frontend/env.js:/app/src/assets/env.js
    networks:
      - inpe_cdsr
    ports:
      - 5001:8000

  inpe_cdsr_catalog_backend:
    container_name: inpe_cdsr_catalog_backend
    image: inpe-cdsr-catalog-backend
    command: python3 -u manage.py run
    # restart: always
    volumes:
      - ../catalog-backend:/app
      - /data/TIFF:/TIFF
    env_file:
      - ./env_files/backend.env
    networks:
      - inpe_cdsr
    ports:
      - 5002:5001

  inpe_cdsr_catalog_dash:
    container_name: inpe_cdsr_catalog_dash
    image: inpe-cdsr-catalog-dash
    # restart: always
    volumes:
      - ../catalog-dash:/app
    env_file:
      - ./env_files/catalog_dash.env
    networks:
      - inpe_cdsr
    depends_on:
      - inpe_cdsr_db
    ports:
      - 5003:8050

  inpe_cdsr_inpe_stac:
    container_name: inpe_cdsr_inpe_stac
    image: inpe-cdsr-inpe-stac
    command: flask run
    # restart: always
    volumes:
      - ../inpe-stac/inpe_stac:/inpe_stac
    env_file:
      - ./env_files/inpe_stac.env
    networks:
      - inpe_cdsr
    ports:
      - 5004:5000

  inpe_cdsr_stac_compose:
    container_name: inpe_cdsr_stac_compose
    image: inpe-cdsr-stac-compose
    command: python3 -u manage.py run
    # restart: always
    volumes:
      - ../stac-compose:/app
      - ./volumes/stac-compose/providers/providers.json:/app/stac_compose/providers/static/providers.json
    env_file:
      - ./env_files/stac_compose.env
    networks:
      - inpe_cdsr
    ports:
      - 5005:5002

  inpe_cdsr_portainer:
    container_name: inpe_cdsr_portainer
    image: portainer/portainer:1.23.0
    # restart: always
    volumes:
      - portainer_data:/data
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - inpe_cdsr
    ports:
      - 9000:9000

  inpe_cdsr_geoserver:
    container_name: inpe_cdsr_geoserver
    image: kartoza/geoserver:2.15.2
    # restart: always
    volumes:
      - ./volumes/geoserver/geoserverdata:/opt/geoserver/data_dir
      - ./volumes/geoserver/grids:/home/data/grids
      - ./volumes/geoserver/web.xml:/usr/local/tomcat/webapps/geoserver/WEB-INF/web.xml
    env_file:
      - ./env_files/geoserver.env
    networks:
      - inpe_cdsr
    ports:
      - 9001:8080

  inpe_cdsr_db:
    container_name: inpe_cdsr_db
    image: mariadb:10.4.11
    # restart: always
    volumes:
      - ../data/mysqldata:/var/lib/mysql
    env_file:
      - ./env_files/db.env
    networks:
      - inpe_cdsr
    ports:
      - 9004:3306

  inpe_cdsr_phpmyadmin:
    container_name: inpe_cdsr_phpmyadmin
    image: phpmyadmin/phpmyadmin:4.9
    # restart: always
    env_file:
      - ./env_files/phpmyadmin.env
    volumes:
      - ./volumes/phpmyadmin/php.ini:/usr/local/etc/php/php.ini
    networks:
      - inpe_cdsr
    ports:
      - 8099:80

networks:
  inpe_cdsr:
    driver: bridge
