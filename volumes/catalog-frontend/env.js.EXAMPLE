(function (window) {

    window.__env = window.__env || {};

    window.__env.urlGeoServer = 'http://localhost:8089/geoserver';

    window.__env.urlViaCEP = 'http://viacep.com.br/ws';

    window.__env.urlStacCompose = 'http://localhost:8089/stac-compose';

    window.__env.urlDGIBack = 'http://localhost:8089/api';

    window.__env.grids = 'States:grid_ibge_states:states; CBERS4-MUX:grid_cbers4_mux:grids; CBERS4A-MUX:grid_cbers4a_mux:grids; CBERS4A-WFI:grid_cbers4a_wfi:grids; LandSat-8:grid_landsat_tm_amsul:grids; Sentinel-2:grid_sentinel_mgrs:grids';

    // search form limit in the left sidebar
    window.__env.searchFormLimit = 1000;

}(this));
